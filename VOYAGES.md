# 远航与野兽 —— 让"探索"和"战斗"不再是死路

## 一、先说诊断:这两件事其实是同一个问题

先把话说清楚:**探索已经有了,战斗几乎没有,而两者会在同一天报废。**

| 系统 | 现状 | 什么时候变成死内容 |
|---|---|---|
| 远征探索 | 3 座手工岛 × 3 个调查点 = **9 个** | 9 个全采完。`expeditionCollected` 进存档,`buildExpeditionWorld(id, collected)` 会把采过的点直接藏掉 —— 第二次登岛是一座**空岛** |
| 图纸 | 4 张(campfire/shelter/collector/dock) | 跟着上面那 9 个点一起结束 |
| 野猪 | 只在夜里出现,追你、蓄力 0.45 秒、咬一口 | **建好灯塔那天**。`beacon.repel === beacon.radius`,护住半座岛,野猪从此再也近不了身 |
| 战斗 | **不存在**。玩家没有攻击手段 | 从第一天起 |

最后一条值得展开:`player.startSwing()` 已经在了,`1H_Melee_Attack_Chop` 动画也在播 —— 但它只砍树。野猪**没有血量**,代码里搜不到任何对野猪造成伤害的路径。现在的野猪不是敌人,是会移动的障碍物。

所以"加战斗"和"加探索"不是二选一。它们烂在同一个地方:**这个游戏里所有东西都是有限的、一次性的。** 建筑建完就没了,清单勾满就没了,图纸拿全就没了,调查点采完就没了。住客系统(见 [RESIDENTS.md](RESIDENTS.md))是第一次尝试解这个结,靠的是"每来一个人就要盖一间屋"。这次要解的是同一个结。

判断标准只有一条:**加进来的东西必须是可再生的。** 否则三小时后我们会再写一份一模一样的文档。

## 二、方案:把远征从"景点打卡"改成"出海一趟"

现在的一次远征是:去 → 采完 3 个固定点 → 回来 → 这座岛永久报废。

改成:去 → 岛上有什么是**这一趟才知道**的 → 带得走多少看你能不能应付 → 回来 → 下次再去是新的。

具体三件事:

### 1. 调查点保持一次性 —— 这是对的,不要动

9 个调查点承载的是**故事和图纸**。故事读过一次就是读过了,图纸拿到就是拿到了。让它们重复刷新只会让"发现"贬值。

**它们本来就该结束。**问题不在它们,在于它们结束之后岛上什么都不剩。

### 2. 每次出海,岛上重新长出可采集的东西

调查点之外,岛上按 `(岛id, 第几次来)` 播种一批**采集点 + 野兽**。同一趟里任何设备上都一样(和漂流物、地形用同一套 `rng`),但下一趟就是另一批。

这一层是可再生的,而且回报要**明显好过在家门口砍树** —— 否则没人愿意出海。

### 3. 岛上有野兽,家里没有

野兽只在远征岛上。这一条是整个设计的支点,原因有三:

- **灯塔管不到那儿。** 主岛的威慑体系是玩家自己建起来的成果,不该反悔。远征岛上没有你的灯塔,所以危险重新成立。
- **主岛保持是"家"。** 长线方向是经营岛屿、最后互相串门。家里天天打架不对。夜里的野猪继续当氛围和早期压力,不升级。
- **"带不带得回来"才有张力。** 载货上限是 4,体力会掉,现在再加一条:路上会被拦。

## 三、战斗做到什么程度

**不做成动作游戏。**参照的是《星露谷》的矿洞,不是《只狼》:野兽是探索的阻力,不是玩法本身。

- **玩家攻击**:复用现有的 `startSwing()` 和 chop 动画。一次挥击,前方一个扇形判定。没有连招、没有闪避、没有格挡。
- **野兽血量**:2–3 下打死。打不过就跑 —— 岛不大,木筏一直在。
- **野兽会预告**:沿用野猪已有的 `windup`(蓄力 0.45 秒 + 脉冲缩放)。看到就能躲开,这是"公平"的最低要求。
- **输了不是死**:体力归零 → 醒在自己床上,**掉一半载货**。不回退天数、不掉建筑、不掉住客。损失是这一趟,不是这个存档。
- **可以不打**:野兽怕火。带火把(或点着的东西)能吓退,代价是占一格载货。**不想打架的人必须有一条路走完全程** —— 长线方向是经营和串门,不是格斗。

## 四、明确不做的

写下来是为了以后不被"顺手加一下"侵蚀:

- 不做武器等级/耐久/词缀。多一套数值不会让挥击更好玩。
- 不做主岛白天的敌人。家就是家。
- 不做 boss。一次性内容,又是死路。
- 不做 PvP、不做访客之间的战斗。访客模式是只读的,这条不能破。
- 不做程序化生成整座远征岛的地形。`expeditionHeight()` 里全是 `if (id === 'reef')` 这样的手工分支,`buildExpeditionWorld` 里道具也是一个个摆的 —— 这 947 行是美术资产,不是配置。**岛是舞台,重新生成的是台上的东西。**

## 五、第一刀切哪儿

一次只做能立刻判断好坏的量:

1. ~~**野兽能被打死**~~ ✅ `src/world/beasts.ts` + `beast-view.ts`,纯逻辑与视图分开
2. ~~**一种野兽:礁蟹**~~ ✅ 慢、正面有壳、绕到侧后才打得动
3. **每趟重播的采集点**:按 `(id, visits[id])` 播种 4–6 个,产出木/石/纤维,量比主岛好 —— 待做
4. ~~**输了的代价**~~ ✅ 血量归零 → 回家 + 掉一半载货(向上取整)

### 做的过程中改掉的两个设计错误

都是"写出来跑一下才发现"的那种,记在这儿是因为它们都不是笔误,是想错了:

**一、野兽生在了登陆点脸上。**第一版按 `(0, -radius*0.8)` 猜登陆点位置,而真正的登陆点在 `activeExpeditionWorld.landing` —— 结果礁岛上有一只蟹守在离木筏 4.6 米的地方,一上岸就进了它的仇恨范围。改成由调用方把真实登陆点传进来,并把安全半径定为"必须 ≥ 最大 aggro"。**在这种地方猜一个公式,必然猜错。**

**二、礁蟹一开始是打不动的。**它每帧都会转到面向玩家,而壳在正面 —— 于是壳永远朝着你,"绕后"这条路根本不存在。加了转身速度上限(2.0 弧度/秒,玩家在攻击距离上绕圈约 4.2 弧度/秒),蓄力期间则完全锁死朝向。

第二条值得单独强调:**"敌人正面有防御"和"敌人始终面对玩家"放在一起,等于"这个敌人无敌"。**测试里现在有一条专门守它 —— 模拟玩家绕圈,断言必须能打到侧后。

这一刀做完就能回答:**在这游戏里打架到底好不好玩。**答案要是"不好玩",第 2、3 条留着(探索本身已经变可再生了),把野兽删掉,损失可控。

顺序是刻意的:先做**能判断的最小量**,而不是先搭框架。

## 六、数据结构

```ts
// src/world/beasts.ts —— 不 import THREE,和 residents.ts 一样保持纯逻辑
export type BeastKind = 'crab';

export interface BeastDef {
  kind: BeastKind;
  name: string;
  hp: number;
  speed: number;
  damage: number;       // 扣多少体力
  windup: number;       // 预告时长,越长越好躲
  drop: Partial<Inventory>;
  scaredByFire: boolean;
}

export interface Beast {
  kind: BeastKind;
  x: number; z: number;
  hp: number;
  windup: number;       // -1 = 没在蓄力
  cooldown: number;
}
```

**不进存档。**野兽和这一趟的采集点都由 `(岛id, visits[id])` 播种,存档只需要那个数字 —— 它已经在 `expeditionVisits` 里了。中途退出重进,岛上还是同一批东西,因为种子没变。这一条同时挡住了"打完退出重进刷掉落"。

## 七、技术决定

- **`beasts.ts` 不 import THREE。**和 `residents.ts` 同样的理由:测试跑得快,而且逻辑能在 Node 里断言。视图放 `beast-view.ts`。
- **命中判定用扇形,不用射线。**低多边形、俯视角、鼠标不参与瞄准 —— 扇形(前方 ±50°、2.2 米)是玩家实际能预期的形状。
- **野兽不进存档,靠种子重建。**理由见上。
- **主岛的野猪不改。**它有自己的角色(夜晚氛围 + 建灯塔的动机),把它改造成"可击杀"会同时破坏这两件事。新的野兽是新东西,不是野猪升级。
- **`main.ts` 已经 2028 行。**这次的接线走 `beasts.ts` + `beast-view.ts`,main.ts 里只留调用。

## 八、还没想清楚的

1. **野兽掉什么?** 掉素材(蟹壳→新材料)会再开一条物品线;掉现有素材则和采集点重复。倾向前者,但要等第一刀跑起来再定。
2. **火把是道具还是状态?** 占一格载货最简单,但载货上限只有 4,占掉四分之一可能太贵。
3. **"掉一半载货"够不够疼?** 太轻就没人躲了,太重就不敢出海。第一版按一半,靠试。
4. **夜里的主岛要不要给玩家反击权?** 现在只能跑。给了,野猪就变成刷子;不给,那把已经在手上的斧头一直是摆设。倾向不给,但这是本文档里我最不确定的一条。

## 九、参照

- **《星露谷物语》矿洞** —— 战斗是下矿的阻力,不是目的;打不过就上楼。本文档的骨架来自这里。
- **《塞尔达传说:织梦岛》** —— 一次挥击 + 敌人有明确预告动作,简单但公平。
- **《动物森友会》** —— 家里绝对安全。这是"主岛不放野兽"那条的来源。
- **《Spiritfarer》** —— 出海一趟带回来的东西喂给家里的系统,而不是自成一套。
