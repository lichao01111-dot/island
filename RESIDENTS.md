# 住客系统 · 设计文档

## 为什么要做这个

不是因为内容不够,是因为**现有的每个系统都会终结,而且没有一个会再生**。

从代码里数得出来:

| 系统 | 总量 | 完成之后 |
|---|---|---|
| 建筑 | 7 种,其中 `dock` / `maptable` / `beacon` 是 `unique` | 建完从建造栏消失 |
| 探索点 | 12 个,一次性发现 | 再去没有新东西 |
| 蓝图 | 4 张 | 集齐即止 |
| 待客清单 | 7 建筑 × 2 + 4 升级 × 1 = 18 分 | **正好满级,之后永远不变** |

`buildings.ts` 里的注释把这件事说得很明白:

> 清单总分刻意配成 `(MAX_ISLAND_LEVEL - 1) * SCORE_PER_LEVEL = 18`:勾满清单正好满级,不多不少。

满级之后:采木头没有用途,去远征没有理由,生存数值只剩维护成本。这就是"到头了"的准确来源。

**所以加第四座远征岛、第八种建筑是无效的** —— 那只是把墙往后挪,墙的性质不变。

需要的是一个**会自己提出新需求的东西**。参照《动物森友会》和《Spiritfarer》:它们的续航引擎不是更多内容,是**会提要求的人**。

## 核心机制

有人漂到岛上,你给他盖房子,他住下来,然后他会**不断提出新的要求**,也会**持续产出**。

### 它盘活了什么

这是选择住客而不是别的方向的**唯一理由**,必须成立:

| 现在的死结 | 住客怎么解开 |
|---|---|
| 建筑建完就没了(`unique`) | 每个住客要一间自己的房子 → 小屋**永不 unique**,建造栏永远有事做 |
| 12 个 POI 一次性 | 住客要材料 → 远征岛从"收集品"变成"产地",要反复去 |
| 待客清单满级即终点 | 岛屿等级**改成住客数量的上限**,从终点变成门槛 |
| 稀有材料只能靠远征 | 织工把纤维变帆布 → 打通一条本地生产线 |
| 客人来了只看到空岛 | 岛上有人住 —— 这才是客人真正想看的东西 |

最后一条尤其重要:社交主线做到现在,访客看到的还是一座无人的岛加一块留言板。**住客让"值得来看"这件事第一次成立。**

## 生命周期

```
漂来 → 在码头等 → 你盖房子 → 住下 → 产出 + 提要求 → 关系变深 → 解锁更多
```

**1. 漂来**
- 复用漂流物的漂近动画,把木箱换成木筏
- 触发条件:岛屿等级 ≥ 门槛,且距上一位住客间隔 ≥ N 天
- **必须有码头** —— 给码头一个持续存在的理由

**2. 等待**
- 站在码头附近,头上有提示
- 靠近对话:说明他会什么、需要什么

**3. 安家**
- 建一间**小屋**(新建筑,非 unique,可重复建)
- 小屋建好后靠近住客 → 他搬进去

**4. 产出**
- 每天产一点东西,堆在小屋外,靠近按 E 收(**复用花圃的 `stock` + 采摘那套**)
- 有上限,不收就停产 —— 逼你回来

**5. 要求**
- 定期提一个要求:「我想要 3 块帆布」
- 材料来自现有系统(采集 / 远征 / 生产)
- 满足后关系加深,产出提升或解锁新东西

## 职业

每个职业要**堵上一个现有的洞**,不能只是风味:

| 职业 | 产出 | 堵的洞 |
|---|---|---|
| 渔夫 | 每天若干食物 | 食物只能靠椰子和花圃,单调 |
| 织工 | 纤维 → 帆布 | 帆布现在只能靠远征,硬依赖 |
| 木匠 | 解锁新建筑 / 降低建造成本 | 建筑树到头了 |
| 水手 | 降低远征体力消耗、提高载货 | 远征后期收益递减 |

**第一刀只做渔夫。**产出最直观,验证成本最低。

## 第一刀的范围

明确地小。目的是**验证方向,不是搭框架**:

1. 第 N 天,一个渔夫漂到码头(硬编码,不做随机生成)
2. 建造栏出现「小屋」(非 unique)
3. 在他附近盖好小屋 → 他搬进去,站在门口
4. 每天在屋外产 1 份食物,最多堆 3 份,靠近按 E 收
5. 提一个要求:3 份纤维。给了之后他道谢,日产变 2 份

跑通这五步就足以判断"有人住的岛"值不值得铺开。

**这一刀明确不做**:对话系统、寻路与作息、多个住客、关系数值、随机生成的人物。

## 数据结构

```ts
export type TradeKind = 'fisher' | 'weaver' | 'carpenter' | 'sailor';

export interface Resident {
  id: string;           // 稳定标识,存档与请求都靠它
  trade: TradeKind;
  name: string;
  arrivedDay: number;
  home: { x: number; z: number } | null;   // null = 还没安家
  stock: number;        // 屋外待收的产出
  growth: number;       // 产出计时(秒)
  request: { kind: ItemKind; count: number; done: boolean } | null;
  favor: number;        // 满足过多少次要求
}
```

存进 `SaveData`,版本升到 4。**v1/v2/v3 都要能读进来**(缺字段就是"还没有住客"),`test/save.mjs` 里补迁移用例。

### 一个必须先定的技术决定

**住客的家不能用建筑数组下标引用。**`buildings` 是个数组,一旦支持拆除,下标会全部错位,住客就会指向别人的房子。

两个选项:

- **A. 存坐标**(上面写的那样),靠近匹配。改动最小,但"哪间屋子是谁的"是模糊判断
- **B. 给建筑加稳定 id**。更干净,但要动存档结构和迁移

**建议 A 做第一刀,B 留到真正需要拆除建筑时再做。**别为了还不存在的需求提前付代价。

## 岛屿等级的含义要改

现在:勾满清单 = 满级 = 终点。

改成:**等级 = 这座岛能留住几个人**。

```
Lv.1-2  → 0 位   还养不活别人
Lv.3-4  → 1 位
Lv.5-6  → 2 位
Lv.7-8  → 3 位
Lv.9-10 → 4 位
```

这样待客清单从"一次性成就"变成"持续有意义的门槛",而且**语义反而更通了**:清单本来就是"客人在你岛上能做什么",能做的事够多,人才愿意留下来。

## 美术

住客需要角色模型。`web/assets/` 里已经有 KayKit 的野蛮人和盗贼,同一套 Adventurers 包里共 5 个角色,CC0。**四个职业刚好能各配一个,不用新建资产。**

复用 `player.ts` 里的 `useModel()` + 动画映射;住客只需要 `Idle` 一个片段,站着就行。

## 开放问题

写在这儿是因为我还没有答案,不是忘了:

1. **住客会不会太吵?**每天都有人要东西,可能从"有人陪"变成"催债"。需要一个"不提要求"的静默期,长度待定。
2. **产出会不会让采集失去意义?**渔夫每天给食物,那椰子树还有什么用?可能要让产出偏向**加工品**(帆布、熟食)而不是原料。
3. **访客能看到住客吗?**应该能,这是整件事的回报。但岛屿码现在只存建筑,加住客要扩快照结构 —— 放第二刀。
4. **住客会不会死/离开?**《Spiritfarer》里角色会离开,那是它情感重量的来源。但对一个轻量经营游戏可能太重。倾向不做。

## 参照

- **《动物森友会》** —— 居民入住、提要求、岛屿评级决定能招几个人。本文档的主要来源。
- **《Spiritfarer》** —— 每个角色有需求链和故事线,住得越久给得越多。
- **《星露谷物语》** —— 送礼提升好感,好感解锁配方与事件。
